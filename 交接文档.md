# 澳股市场雷达 — 交接文档

> 给新对话看的。项目在 `D:\CLAUDE CODE\asx-quant`，本地跑 `python run.py`（端口 8849）。
> 姊妹项目：`D:\CLAUDE CODE\hk-quant`（港股版，端口 8848，架构相同）。
> 最后更新：2026-08-23

---

## 一、这是什么

澳洲股市分析工具。**四层输出，按证据强度排序**，界面上直接标注每层可信到什么程度：

1. **顶部资金流面板**（第一屏）— 钱正在进哪个板块的哪几只股票 / 正在从哪撤。**测量，不是预测**
2. **做多/做空三只名单** — 看空侧统计显著，看多侧未通过检验，界面明说
3. **板块全景** — 资金 / 热度 / 延展度 / 做空压力 四个独立分数
4. **大盘概率** — 方向基本不可预测，波动率可预测

全部界面是**大白话 + 术语鼠标悬停解释**（用户不懂金融，这是硬要求）。

---

## 二、已经跑过的检验，结论不要推翻重来

这些是这个项目最值钱的部分。**不要在没有新证据的情况下改变这些结论。**

| 检验对象 | 结果 | 判决 |
|---|---|---|
| 个股**看空**评分（横截面） | IC **t = −7.3** | ✅ 显著有效 |
| S4 吸筹结构（60日相对板块） | IC +0.0138, **t = 6.93**，五分位价差 +0.72% | ✅ 通过，已给权重 |
| 20日**波动率**方向 | AUC **0.717**, BSS **+0.143** | ✅ 全系统最强 |
| 个股**看多**评分 | IC t = +1.16 | ❌ 不显著 |
| **板块**空头变化 → 未来板块超额收益 | 滞后相关峰值在 **lag +4 周** | ❌ 落后于价格，无领先性 |
| 大盘 5/10/20 日方向 | AUC 0.45–0.51 | ❌ 无预测力 |

**两条必须一直保留的表述：**
- 看空评分衡量的是**「跑输」不是「下跌」** —— 样本期内空头篮子自身仍涨 +2.15%/年（市场 +12.67%）
- **空头信号衰减很快**：20日再平衡跑输 −10.5%/年，40日 −6.5%，60日 +1.9%（失效），120日 +5.1%（反转轧空）→ **名单约每月刷新**

---

## 三、数据源与已知硬限制

| 数据 | 来源 | 覆盖 | 注意 |
|---|---|---|---|
| 行情 | Yahoo `.AX` | 153只 + 指数/商品，12年 | — |
| **空头持仓** | ASIC 年度汇总 CSV | **1,168交易日 × 2,591只**（2022起） | T+4 发布，已对齐到公开日 |
| 公告 603/604/605/3Y | ASX markitdigital API | **每家只返回最近5条** | ⚠️ 无法回溯，见下 |

**ASIC 空头 URL**：`https://download.asic.gov.au/short-selling/RR{YYYYMMDD}-001-SSDailyYTD.csv`
（拉 5 个年度文件即可得 4.6 年，约 6 秒）

**ASX 公告 PDF**：`https://asx.api.markitdigital.com/asx-research/1.0/file/{documentKey}?access_token=83ff96335c2d45a094df02a206a39ff4`

### ⚠️ 三个已修复的数据陷阱 — 改代码时绝对不要改回去

1. **除息假跌**：澳股股息率 4–6% 且集中派发，未复权价在除息日出现假跌。实测 14% 的股票 60日收益偏差 >1pp，最大 8.59pp，受影响最重的正是银行和 REITs（最容易被误判为"资金流出"）。GPT 从 −2.07% 变 +0.32%，**符号反转**。
   → **所有跨日指标用复权价**（收益率、均线距离、RSI、52周位置、广度）；**只有日内指标用原始 OHLC**（CMF、MFI、ATR、成交额），因为它们单日内自洽。

2. **S4 不能写成四项连乘**：规格明确警告方差爆炸且 Amihud→0 时发散。必须**各项先取对数、独立截尾（±1.5）、再相加取平均**。

3. **Yahoo 幽灵交易日**：会给当前日历日发一行只有极少标的的假数据。已按"非空标的数 < 20%"剔除，实测剔掉 221 行。

---

## 四、累积机制（用户最新需求，已实现）

> 用户原话：「每次一点更新，系统就自己将新的资料拉取进去软件里面，这样日积月累推测才会准确」

**只有两样东西无法事后重建，所以必须累积：**
1. **公告** — API 只给最近5条，错过就永久丢失
2. **每天模型说了什么** — 信号是用当天数据算的，事后无法复现

实现在 `asxquant/archive.py`，存进 `cache/announcements.db`（SQLite，4张表）：
- `filings` — 公告存档（已剔除 Vanguard/BlackRock 被动持仓、证券借贷名义持仓）
- `sector_snapshots` / `stock_snapshots` / `market_snapshots` — 每日信号定格

**⚠️ 关键设计决定（不要改回去）：这个 .db 是故意提交进 git 仓库的。**
GitHub Actions 缓存约 7 天不用就清空，把存档放缓存里会被定期清零，正好毁掉累积的意义。
`.gitignore` 里有 `!cache/announcements.db`，workflow 里有「回写累积存档」步骤（权限 `contents: write`）。

界面上有累积进度条：**攒够约 60 个交易日**就能跑规格要求的 F1 领先性检验，通过了才把公告数据纳入评分。
当前：1 天快照 / 152 条公告 / 进度 2%。

---

## 五、部署现状

| 项 | 值 |
|---|---|
| GitHub | `lokliaoau-bot/asx-radarr`（**公开**仓库） |
| 手机网址 | `https://lokliaoau-bot.github.io/asx-radarr/` |
| 自动更新 | `.github/workflows/daily.yml`，UTC 08:30 工作日 |
| 本地 | `python run.py` → 8849，会打印手机可用的局域网地址 |

**用户当前卡在的位置**：代码已推送（commit `40dbf55`），**还差两步、都要在网页上点**：
1. Settings → Pages → Source 选 **GitHub Actions**
2. Actions → 每日更新澳股雷达 → **Run workflow**（首次 3–6 分钟）

首次云端运行没验证过，可能踩坑：Linux 环境差异、依赖版本、Actions 权限。失败要看具体步骤名。

### 三种交付形态（共用一套前端）
- **本地服务器** `python run.py` — 点更新=真的重抓数据+算模型
- **云端 PWA** `python run.py --publish` → `site/` — 手机装主屏幕，点更新=取最新自动结果
- **云盘单文件** `export/ASX市场雷达.html`（约 360KB，全内嵌，双击即开）

`app.js` 先试 `api/report`，失败回落 `report.json`，所以一套前端同时服务本地和静态托管。

---

## 六、代码结构

```
asxquant/
  config.py        153只股票池、11个GICS板块、模型参数
  datafeed.py      Yahoo + ASIC 抓取与缓存
  indicators.py    技术/广度/资金流指标
  features.py      43个市场因子（含空头持仓块、大宗商品块）
  sectors.py       板块合成 + 资金/热度/延展/做空 四个评分
  smartmoney.py    S4吸筹信号 + 顶部资金流入流出面板   ← v2新增
  announcements.py ASX公告采集 + PDF解析（暂不打分）     ← v2新增
  archive.py       每日快照存档（累积机制核心）          ← v2新增
  picks.py         个股多空评分 + 三只名单
  validate.py      横截面IC / 五分位 / 多空组合验证
  model.py         走向前建模、Platt校准、按技能收缩
  engine.py        编排，产出 JSON
  export.py        单文件HTML导出    publish.py  静态站点生成
  server.py        本地服务（仅标准库，绑 0.0.0.0 供手机访问）
web/               前端 + PWA（manifest / sw.js / 图标）
```

**建模纪律**（全部已实现，别拆）：扩张窗口每21日重训 · 净化禁运 t−h−5 · 三模型等权组合 · 滚动 Platt 校准 · 按实测 AUC 收缩（AUC≤0.5 → λ=0 直接报基准率）。

---

## 七、用户偏好（重要）

1. **不懂金融** — 结论必须大白话，术语用鼠标悬停解释（已实现 `term()` / `annotate()` / `GLOSSARY`）
2. **要诚实** — 没优势就说没优势，绝不制造虚假信心。每个数字旁边附样本外成绩单
3. **一键更新** — 缓存要狠，重复刷新以秒计
4. **跨设备** — 手机 + 云盘都要能看

---

## 八、可以继续做的事

- **等 60 天后**跑公告数据的 F1 领先性检验，通过才纳入评分（这是规格定的判决点）
- 规格里还没做的：F3 板块融资额（配售折价，Appendix 2A/3B）、S6 结构性事件日历
- 规格明确说**不要做**的：ASX 个股期权流（流动性太低）、板块ETF申赎（11个板块只有4个有本土ETF，全球ETF推断澳洲资金流是错的）
- 港股版 `hk-quant` 没有累积机制（南向数据只能回溯64天，同样的问题），可考虑移植 `archive.py`

---

## 九、参考

两份原始规格：`D:\Download\asx-smart-money-detector-spec.md`、`asx-sector-flow-tracker-spec.md`

文献：Welch & Goyal (2008) · Campbell & Thompson (2008) · Rapach, Strauss & Zhou (2010) ·
Gu, Kelly & Xiu (2020) · Asquith, Pathak & Ritter (2005) · Boehmer, Jones & Zhang (2008) ·
Lee & Ready (1991) · Amihud (2002) · Corsi (2009) · López de Prado (2018) · de Kempenaer (RRG)
