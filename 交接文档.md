# 澳股市场雷达 — 交接文档

> 给新对话看的。项目在 `D:\CLAUDE CODE\asx-quant`，本地跑 `python run.py`（端口 8849）。
> 姊妹项目：`D:\CLAUDE CODE\hk-quant`（港股版，端口 8848，架构相同）。
> 最后更新：2026-08-24

---

## 一、这是什么

澳洲股市分析工具。**四层输出，按证据强度排序**，界面上直接标注每层可信到什么程度：

1. **顶部资金流面板**（第一屏）— 钱正在进哪个板块的哪几只股票 / 正在从哪撤。**测量，不是预测**
2. **做多/做空三只名单** — 看空侧统计显著，看多侧未通过检验，界面明说
3. **板块全景** — 资金 / 热度 / 延展度 / 做空压力 四个独立分数
4. **大盘概率** — 方向基本不可预测，波动率可预测

全部界面是**大白话 + 术语鼠标悬停解释**（用户不懂金融，这是硬要求）。

---

## 二、已经跑过的检验，结论不要推翻重来

这些是这个项目最值钱的部分。**不要在没有新证据的情况下改变这些结论。**

### ⚠️ 先读这条：本项目的 t 值必须做重叠修正（2026-08-24 修复）

前瞻收益是 20 日（或 60 日）的，却**每个交易日都算一次**，相邻观测的收益窗口几乎完全重合。
实测 IC 序列 lag-1 自相关 **+0.90**。把它当独立样本算 t 值，**标准误会低估 3–5 倍**。

`validate.py` 的文档从一开始就写着用 Newey-West，**但代码里一直是最朴素的 t 值** —— 已修复。
现在 `t_stat` 就是 Newey-West(lag=horizon−1) 修正后的值，`t_naive` 保留原值仅供对照，
另外还给了「把序列拆成 horizon 组互不重叠的子样本」的交叉检验。

**下面表里的 t 一律是修正后的。任何人再报一个两位数的 t 值，先问他有没有做重叠修正。**

| 检验对象 | 修正后 t | （未修正） | 判决 |
|---|---|---|---|
| 个股**看空**评分（20日横截面） | **−2.20** | −7.24 | ⚠️ 勉强达标（边缘显著） |
| 看空评分**五分位价差** −0.93%/20日 | **−2.11** | −6.65 | ⚠️ 同上 |
| 20日**波动率**方向 | 集成 0.713 已弃用；**改用校准后的波动率水平：同样本 AUC 0.771, BSS +0.171, λ=1** | — | ✅ 全系统唯一真实发布的概率（来源标注在界面上） |
| 个股**看多**评分 | +0.31 | +1.07 | ❌ 不显著 |
| **S4 吸筹**（生产用的滚动z形式，60日） | **+0.45** | +2.25 | ❌ **已撤权重**（原记录 t=6.93 是未修正值） |
| S4 吸筹（原始值，60日，即当年那次检验） | +1.07 | +5.43 | ❌ 复现了当年的 IC(+0.0134)，但修正后不显著 |
| **上方套牢盘占比**（成交量分布，60日） | +1.58 | +7.67 | ⚠️ 只显示不打分 |
| **量价背离**（CVD 的日线版，20日） | −1.27 | −3.94 | ❌ 不采用（**无证据**，不是"信号反向"） |
| **轧空压力**（空头浮亏度→未来收益，2026-08-25） | **−0.09** | −0.32 | ❌ 完全无预测力（与20日动量相关 +0.54，是动量马甲）→ 维持只展示 |
| **回补天数20日变化**（2026-08-25） | −1.85 | −5.64 | ❌ 未过门槛（且与空头变化高度同源） |
| **加空×强势交互项**（2026-08-25） | −0.90 | −2.22 | ❌ 未过门槛 |
| **板块**空头变化 → 未来板块超额收益 | 滞后相关峰值在 **lag +4 周** | — | ❌ 落后于价格，无领先性 |
| 大盘 5/10/20 日方向 | AUC 0.45–0.51 | — | ❌ 无预测力 |

**2026-08-25 审计发现并已修复：`validate.py` 之前验证的不是生产评分。**
生产 `picks.py` 的看空评分含 `squeeze_safe = -z(回补天数)`（权重 0.08），
但验证里这一项一直是**全零占位** —— 等于 t=−2.20 验的是一个略有出入的组合。
已把 DTC 时间序列接进 `build_factor_panel(short_shares=)`，忠实复刻后 **t=−2.22**，
判决不变（运气好：出入很小），但以后**改 picks.py 权重必须同步改 validate.py**，
两边不一致时验证数字毫无意义。

**三条必须一直保留的表述：**
- 看空评分衡量的是**「跑输」不是「下跌」** —— 样本期内空头篮子自身仍涨 +2.48%/年（市场 +12.86%）
- **空头信号衰减很快**：20日再平衡跑输约 10pp/年，40日约 6pp，60日失效，120日反转（轧空）→ **名单约每月刷新**
- **真按它做市场中性组合，扣成本后年化只剩 +0.60%、夏普 0.04，一年换 39 批仓** ——
  所以它的用途是**回避/少配**，不是**靠做空赚钱**。这句话现在已经写在界面上了。

> 为什么看空侧还留着：组合回测**每 20 日换一次仓，本来就不重叠**，
> 空头篮子确实跑输大盘约 10pp/年——这条独立证据不受重叠问题影响。
> 被推翻的是"t=−7.3、可信度强"这个**说法**，不是这个信号本身。

### 💣 全系统「最强」的波动率模型，输给一行算术（2026-08-25 实测）

项目引了 **Welch & Goyal (2008)**，那篇论文的核心论点就是「花哨的预测器打不过朴素基线」，
**但项目一直没做这个检验** —— 每条预测只跟「抛硬币/历史平均」比，从不跟显然的一行替代方案比。

在**完全相同的 1,931 天**上：

| 方法 | AUC |
|---|---|
| 43因子 + 走向前 + Platt校准 + 三模型组合 | 0.713 |
| **`-realized_vol`：就看「现在波动率低不低」** | **0.775** |
| 波动率 756 日分位取负 | 0.758 |
| 波动率 504 日分位取负 | 0.751 |

**四个分段全输**（2017-19 / 2020-21 / 2022-23 / 2024-26），不是某一段撑起来的。
道理也简单：波动率会均值回归，「现在低 → 将来升」本身就是强预测，模型反而把它做没了。

**回撤模型更糟**：AUC **0.31 / 0.37 —— 比抛硬币还差**，而同期「只看当前波动率」能拿 **0.62**。
也就是说回撤**确实可预测**，是这套模型把信号做反了。（这个反向现象值得单独查，还没查。）

**2026-08-25（Fable 接手）第二阶段处置 —— 挑战者机制：**

同样本 AUC 对决（vol_up_20d，n=1931 同行）：裸 `-rv` **0.775** > HAR-RV 0.766 >
加了水平特征重训的集成 0.731 > 现集成 0.713。**HAR 和特征重训都测了、都输了、都没上线**
（记录在 `engine._targets` 注释里，别再重做）。

新机制（`model.score_to_prob_expanding` + `engine._fit_target` 挑战者段）：
- 每个风险目标带 `candidates`（原始分数），走净化扩张逻辑回归转成无泄漏概率
- 接管条件（在模型自己的评估行上）：**BSS>0 且 AUC 高于集成** —— 双门槛
- 波动率：接管成功，界面真实发布 P(更颠簸)（λ=1，BSS +0.17），标注「已改用：当前波动率水平」
- 三个回撤目标：候选全被 BSS 门槛拒掉（0.62 的原始区分度经无泄漏校准后 BSS 仍为负——
  74 个正例挤在 4 场危机里，连条件概率都标不准）→ **报基准率就是正确答案**，
  candidates 字段留痕。**别试图再给回撤上模型，先想清楚为什么条件概率都失败**

**已做的处置：**
1. `_targets` 里每个风险目标带一个 `naive` 朴素基线，`model.naive_auc()` 在**与模型完全相同的评估行**上算它的 AUC
2. `_skill_verdict` 加一条硬规则：**跑输基线 → 直接判「无」**，AUC 再高也没用
3. `shrink_to_base(..., naive_auc=)`：跑输基线时 **λ 直接归零**，界面改报历史平均值，
   不再拿一个不如一行算术的模型概率糊弄人
4. 界面预测表加一列「**不用模型能到多少**」

**结论：大盘那一层（方向 + 风险）现在没有一条能打过朴素基线。**
方向本来就判过「不可预测」；风险这一层过去被当成全系统最强，现在也倒了。
下一步要么放弃这层、要么直接改用基线，别再拿现在这套模型当卖点。

---

### 关于「拿一份 tick 级订单流代码来优化」这件事（2026-08-24 已评估，结论别推翻）

有人给过一份 FastAPI + Redis 的 ASX 智能资金追踪代码（显性大单 + 冰山拆单 + CVD 背离 → 打分）。
**它的核心跑不了**：全部依赖逐笔成交 + 买卖方向标记（bs_flag），也就是 ASX Level-2 订单流。
Yahoo 只给日线，ASX 也不公开买卖方向；那份代码里的数据源是 `generate_mock_asx_ticks` —— 模拟数据，从没接过真行情。
真数据要 IRESS / Refinitiv / Databento，月费四位数澳元。**除非买数据，否则不要再试图移植大单/冰山那一套。**

从里面**实际迁移过来的只有一样**：成交量分布（POC / 价值区 / 上方套牢盘），改写成日线可算的版本。
另外两个想法都检验过并否决了：CVD 背离修正重叠后 t=−1.27，**没有任何统计证据**
（注意措辞：2026-08-24 之前这里写的是"符号是反的"，那是基于未修正的 t=−3.94，说过头了。
准确说法是——**既没有证据支持原代码的看涨用法，也没有证据支持反着用**）；
VWAP 偏离和现有 `dist_from_ma` 高度重合（`indicators.vwap_dev` 至今没人调用）。
那份代码的打分体系（50 分起、±20/±10/±15 硬编码）**没有任何验证**，与本项目「没实测优势就不给权重」的规矩直接冲突，没有采用。

---

## 三、数据源与已知硬限制

| 数据 | 来源 | 覆盖 | 注意 |
|---|---|---|---|
| 行情 | Yahoo `.AX` | 153只 + 指数/商品，12年 | — |
| **空头持仓** | ASIC 年度汇总 CSV | **1,168交易日 × 2,591只**（2022起） | T+4 发布，已对齐到公开日 |
| 公告 603/604/605/3Y | ASX markitdigital API | **每家只返回最近5条** | ⚠️ 无法回溯，见下 |

**ASIC 空头 URL**：`https://download.asic.gov.au/short-selling/RR{YYYYMMDD}-001-SSDailyYTD.csv`
（拉 5 个年度文件即可得 4.6 年，约 6 秒）

**ASX 公告 PDF**：`https://asx.api.markitdigital.com/asx-research/1.0/file/{documentKey}?access_token=83ff96335c2d45a094df02a206a39ff4`

### ⚠️ 三个已修复的数据陷阱 — 改代码时绝对不要改回去

1. **除息假跌**：澳股股息率 4–6% 且集中派发，未复权价在除息日出现假跌。实测 14% 的股票 60日收益偏差 >1pp，最大 8.59pp，受影响最重的正是银行和 REITs（最容易被误判为"资金流出"）。GPT 从 −2.07% 变 +0.32%，**符号反转**。
   → **所有跨日指标用复权价**（收益率、均线距离、RSI、52周位置、广度）；**只有日内指标用原始 OHLC**（CMF、MFI、ATR、成交额），因为它们单日内自洽。

2. **S4 不能写成四项连乘**：规格明确警告方差爆炸且 Amihud→0 时发散。必须**各项先取对数、独立截尾（±1.5）、再相加取平均**。

3. **Yahoo 幽灵交易日**：会给当前日历日发一行只有极少标的的假数据。已按"非空标的数 < 20%"剔除，实测剔掉 221 行。

---

## 四、累积机制（用户最新需求，已实现）

> 用户原话：「每次一点更新，系统就自己将新的资料拉取进去软件里面，这样日积月累推测才会准确」

**只有两样东西无法事后重建，所以必须累积：**
1. **公告** — API 只给最近5条，错过就永久丢失
2. **每天模型说了什么** — 信号是用当天数据算的，事后无法复现

实现在 `asxquant/archive.py`，存进 `cache/announcements.db`（SQLite，4张表）：
- `filings` — 公告存档（已剔除 Vanguard/BlackRock 被动持仓、证券借贷名义持仓）
- `sector_snapshots` / `stock_snapshots` / `market_snapshots` — 每日信号定格

**⚠️ 关键设计决定（不要改回去）：这个 .db 是故意提交进 git 仓库的。**
GitHub Actions 缓存约 7 天不用就清空，把存档放缓存里会被定期清零，正好毁掉累积的意义。
`.gitignore` 里有 `!cache/announcements.db`，workflow 里有「回写累积存档」步骤（权限 `contents: write`）。

界面上有累积进度条：**攒够约 60 个交易日**就能跑规格要求的 F1 领先性检验，通过了才把公告数据纳入评分。
当前：1 天快照 / 152 条公告 / 进度 2%。

---

## 四点五、成本地图（2026-08-24 新增，只显示不打分）

`indicators.volume_profile()` —— 把过去 60 个交易日每天的成交量按「当天最高–最低」均匀摊到各个价位上
（日线没有价位成交明细，这是标准近似），得出：

- **POC** 成交最密集的那个价位
- **价值区** 约占七成成交量的价格区间
- **平均成本** 成交量加权的大众成本
- **上方套牢盘占比** 现价之上还压着多少成交量

⚠️ **必须在复权价空间里算**（第三节陷阱1 的同一个坑）：澳股股息集中派发，
用原始价算会让高股息的银行/REITs 凭空多出「套牢盘」。算完再折回今天的报价显示。

**它是测量，不参与任何评分**——理由见第二节表格：看着很强的 t=7.67 是**未做重叠修正**的，
Newey-West 修正后只有 +1.58，不重叠交叉检验 +1.15，**从来就没有稳健证据**（不是"近期才失效"）。
按项目规矩（没实测优势就不给权重），只能出现在卡片上，不能进分数。前端在 `costMap()`。

其它几条实现上的硬要求（都是 2026-08-24 审查时踩过的）：
- **价格锚点必须与复权因子取自同一行**，不能分别 `dropna().iloc[-1]`，否则 close/adjclose
  缺失模式一旦不同就会静默串日期。
- **有效交易日不足 40 天就不出图**（`MIN_PROFILE_DAYS`）：40 个分箱配二十来天数据，POC 纯属噪音。
- `cost` 用 `(H+L)/2` **不是** `(H+L+C)/3`：直方图假设当日成交量在 [L,H] 上均匀分布，
  该假设下当日均价就是中价，换成典型价会和直方图自相矛盾。
- 成交量**均匀**摊开、不做靠近收盘的加权：那属于没验证过的建模，不该加在测量层上。
- `edges` **不进报告**：它就是 `linspace(lo, hi, len(hist)+1)`，前端自己还原。

---

## 四点六、⚠️ 改前端/改报告字段前必读：PWA 缓存陷阱（2026-08-24 踩过）

原来的 `web/sw.js` 对外壳（`app.js` / `style.css`）用的是**缓存优先**，而 `report.json` 是**网络优先**。
结果每次部署后第一次打开，都是**上一版的 app.js 去渲染这一版的 report.json**。

平时只是样式旧一轮看不出来。但只要报告里**增删过字段**，旧脚本就会抛异常——
而 `load()` 的 `.catch` 又把渲染异常伪装成「尚无数据。点击右上角更新全部指标」，
于是表现为**整页空白 + 一句误导人的提示**，看起来像后端没数据，实际是前端缓存问题。

已修三处：
1. `sw.js` 缓存名 `asx-radar-v1` → **`v2`**（activate 会清掉旧缓存），
   外壳也改成**网络优先、失败回落缓存**——脚本和数据必须是同一代产物。
2. `load()` 区分「取不到数据」和「渲染崩了」，后者直接提示**强制刷新**并显示真实错误。
3. `costMap()` 对缺字段返回空串，一张卡片不再拖垮整页。

**以后再从报告里删字段，记得 sw.js 缓存名一起改。**

---

## 四点七、空头数据的四个渠道 —— 都查过了（2026-08-24，别再重查）

用户问「时效性从慢到快的四个空头渠道你都考虑了吗」。逐个实测的结论：

| 渠道 | 延迟 | 测什么 | 实测结论 |
|---|---|---|---|
| **ASIC 空头头寸** | T+4 | 存量 | ✅ **已用**。唯一免费可自动化的空头数据源 |
| **ASX 每日毛空头卖出** | T+1 | 流量 | ❌ **已搬进 ASX Online 参与者门户，要券商账号登录**。不再是免费公开数据 |
| **Cboe 独有产品** | T+1 | 流量 | ❌ **与本股票池零重叠**，见下 |
| **停牌 / 公告** | 近实时 | 事件 | ⚠️ 公告已抓；**停牌改用价格停更检测**，已实现 |

**ASX 毛空头卖出**：`www.asx.com.au/data/shortsell.txt` 这个老地址已废弃（重定向到首页），
且整个 asx.com.au 有 Imperva 机器人防护，纯 HTTP 拿不到。
从 sitemap 里找到现址 `/participants/participants-resource/short-sales-reports`，
它会跳到 **asxonline.com**（参与者门户），分类过滤器里明确有 "Short Sales Report"，
但未登录时 No Results。**这是券商专用数据，零售拿不到。**

**Cboe**：`cboe.com.au` 现在跳转到 **tmxaustralia.com**（Cboe 澳洲已被 TMX 收购改名）。
它的 Gross Short Sales Reports 页面还在，但页面法律条文写明：
「**ASX has been nominated by TMX Australia Exchange as its agent for the submission of
the short sale information ... in respect of ASX listed products**」——
即 **ASX 上市品种在 TMX 成交的空头，由 ASX 代为申报**，TMX 自己那份只覆盖「Cboe/TMX 独有产品」
（权证、其自有上市品种）。本项目 153 只全是 ASX 大中盘股，**交集为零**。

**ASIC 的其它文件**：`download.asic.gov.au/short-selling/short-selling-data.json` 是可用日期索引
（4093 个交易日 + version 号）。`SSDailyAggShortPos.csv` 只是同一份 T+4 存量的单日版。
**ASIC 只发存量，不发流量。**

⚠️ **2026-08-25 修正一个错误认知**：之前以为修订版与 001 版"只差几十字节"无关紧要——
实测 20260427 的 v001 vs v002：**97,881 行里 43,671 行不同，GYG 空头占比 12.08% → 14.01%**
（差近 2 个百分点！字节数相近只是巧合）。`datafeed._asic_index()` 现在用索引取**最新修订版**，
同时免掉了逐日试探（最多 20 次 3MB 请求 → 1 次 139KB 索引），最新日期也不再靠猜。
逐日试探仍保留为索引失败时的回退路径。

**2026-08-25 补测的三个候选因子全军覆没**（NW 门槛 t≥2.0，见第二节判决表）：
轧空压力 −0.09、回补天数变化 −1.85、加空×强势交互 −0.90。对照组里现有的
「空头20日变化」是 −2.00 —— **ASIC 数据里能榨的信息，现有评分已经榨完了**。
别再从同一份数据里造新因子，下一个真增量只能来自新数据（公告 F1 攒够 60 天）。

---

## 四点八、空头成本估算 `shortcost.py`（2026-08-24 新增，只显示不打分）

用户想要「空头叫什么名字、他在什么价位加的仓、他的均价多少」。

**必须一直保留的澄清：澳洲不公开做空者身份。**
ASIC 的文件只有 `date, code, short_shares, short_pct` 四列，是**全市场汇总**。
欧盟/英国（ESMA、FCA）会公布超过 0.5% 的空头持有人姓名，日本也有，**澳洲没有**。
603/604/605 公布的是**多头** 5% 以上的实益持有人，与空头无关
（对冲基金偶尔出现在 604 里，那通常是证券借贷腿，不是披露的空头）。
所以「哪家基金、他的成本」在澳洲**无解**，不是代码问题。

**能重建的是全市场空头的平均建仓价**：把总空头仓位当成一个 FIFO 存货——
某日仓位增加＝有人在当日典型价 (H+L+C)/3 开了新空单，减少＝最早的空单被平掉——
即可倒推出**当前仍未平仓那部分**的加权平均建仓价，以及空头目前浮盈还是浮亏。
**空头浮亏是轧空的前提条件**，这正是用户真正想要的东西。

实测把关：
- **估算成本必须落在建仓期价格区间内** —— 110 只全部通过
- **可追溯覆盖率**中位 92%，但有 22 只低于 80%（CHN 39%、BOQ 39%）——
  那是 2022 年 ASIC 数据开始前就存在的老仓位，永远无法定价。**界面必须显示这个比例。**
- 全表扫描改 groupby 后 8.8s → 1.15s

与成本地图一样：**测量，不参与任何评分**（而且按第二节的教训，也别拿未修正的 t 值去给它背书）。

### 空头盈亏公式（2026-08-25 修正，别改回去）

`pnl = 1 - px_now / cost`，**相对建仓本金**算的，不是 `cost/px - 1`。
后者是相对现价算的，会**系统性低估亏损**：在 A$10 做空、涨到 A$20 是亏掉 100% 本金，
它只报 −50%。错的方向最要命——空头亏得越狠、被迫回补的压力越大，
这个功能的全部意义就是发现这种情况，结果被算小了一半。
实测影响：NEU −39.8% → **−66.1%**，GMD −33.1% → −49.6%，CSL −27.9% → −38.7%。
（排序不受影响，两个公式对 px/cost 都单调；变的是幅度。）

**做空亏损没有上限、可以超过 100%**（做多最多亏光本金，做空不是）。界面与术语表都写了。

已跑过的边界检验（110 只全过）：估算成本落在建仓期价格区间内、
加仓价落在当日高低价之间、无 cost≤0、无 |pnl|>300%、as_of 全体一致、无仓位归零重建。

---

## 四点九、停牌 / 退市检测（2026-08-24 新增）

`datafeed.trading_status()` / `halted_tickers()`：拿每只股票最后一根有效收盘 K 线
和大盘最新交易日比对。不解析公告，所以停牌、退市、数据中断全都能抓到。

**发现的真实问题**：QUB（Qube Holdings）已从 ASX 退市
（ASX 公告接口返回 `Symbol not found`，全池 153 只只有它这样），
最后成交 2026-08-18，但系统一直把 A$5.11 当作现价、还让它有资格进推荐名单。
现在它会被自动排除，并在界面「已停止成交」处列明。

⚠️ **不要把退市股从 `config.py` 里删掉**。README 里写的「已剔除退市标的」其实是个坏习惯：
删掉会让回测只在活下来的股票上跑，**加重幸存者偏差**。
Yahoo 本来就把退市后的价格留成 NaN，回测的掩码会自动排除它；
真正的 bug 只在于实时卡片用 `safe_last` 取到了那个陈旧价格——现在已经堵上了。

## 四点九点五、Platt 校准会偷偷把模型反着用（2026-08-25 修复，别改回去）

Fable 留了一句「回撤模型 AUC 0.31 系统性反向，值得单独查」。查清楚了，是**校准层的 bug**。

**先纠正一半结论**：块自助法 95% 区间显示，三个回撤目标里**只有 dd_4_10d 是真反向**
（AUC 0.314, CI [0.212, 0.445] 不含 0.5，149 个正例）；dd_5_20d 和 dd_8_20d 的区间都跨过 0.5
（dd_8_20d 只有 74 个正例，CI 宽达 [0.17, 0.67]）——**那两个是噪音，别当结论**。

**机制**：`calibrate_expanding` 用逻辑回归做 Platt 缩放，但从没约束斜率符号。实测
dd_4_10d **91%** 的时间块拟合出**负斜率**，dd_5_20d 100%、dd_8_20d 99%（波动率目标 0%）。
负斜率 = 校准器判定"这模型是反的"，于是**把它的输出倒过来发布**。
这不是校准，是偷偷做了模型选择：Platt 缩放的定义就是**单调递增**映射，
一旦顺序被反转，"校准后的概率"就不再是模型主张的东西。
dd_4_10d 因此从原始集成 0.502（诚实的没用）变成校准后 0.314（自信的错误）。
而无区分度模型的样本内符号本来就是噪音，照它翻转等于凭空制造信心。

**修法**：斜率 ≤ 0 时**整块留空**，不发布任何概率。
⚠️ 中途试过"留空处退回基准率"，**更糟**：扩张基准率会随历史累积漂移，
而回撤事件在时间上聚集，光是这个漂移就刷出 AUC 0.24–0.38 的假信号。
**会变的占位符不是占位符，是一个新信号。**

**修完的效果（不止回撤）**：dd_4_10d 0.314 → 0.550（反向消失）；
三个方向目标也一起改善（5日 0.515→0.537、10日 0.491→0.526、20日 0.450→0.497）——
说明它们也一直有部分时间块在被反着用。

`engine._fit_target` 相应加了 `has_current`：校准整块留空意味着**今天没有预测**，
不能拿几个月前仅存的那个值冒充今天的概率。

### ⚠️ 同时修掉的一个连带 bug：挑战者门槛和记分用了不同样本

校准留空之后，模型的有效行可能只剩一小撮（dd_4_10d 只剩 209 行）。
Fable 的挑战者机制在**那 209 行**上判门槛，晋级后却在候选自己的 **2692 行**上记分——
结果 dd_4_10d 晋级了一个**实际 BSS = −0.039** 的候选，还被判成"微弱预测力"。
现在：**Brier 门槛在候选自己的完整样本上跑**（就是将来公布指标的那批行），
**AUC 对比在两者都覆盖的共同样本上跑**。修完三个回撤候选全部被正确挡下。

### 报告的 BSS 必须是「收缩后」的那个

`evaluate` 打分的是**收缩前**的序列，但用户看到的概率是 `base + λ×(p−base)`。
两者可以跨零：dir_5d 收缩前 −0.0032、收缩后 **+0.0003**；dir_10d −0.0050 → **+0.0002**。
救活它们的正是收缩，所以该被打分的也是收缩后的东西。
`model.published_skill()` 负责算，界面显示的就是它（鼠标悬停能看到收缩前的值）。

顺带当安全网：**收缩后仍不及气候值就直接停发**（λ 归零）。目前没有目标触发这条，
但以前系统对挑战者要求 BSS>0、对自己却不查，这个不一致已经补上。

---

## 四点十、三个「自己坑自己」的教训（都真踩过，别重蹈）

**1. 用 heredoc 喂 Python 改文件时，字符串里的 `
` 会变成真换行。**
这一天里踩了两次：`publish.py` 的 `newline="
"` 和 `app.js` 的 `join("
")`
都被写成了**真的换行符**，后者直接让 `app.js` 语法错误、整页打不开。
**规矩：内容里带反斜杠转义的，用 Edit 工具改，不要走 heredoc→Python→文件这条链。**

**2. `app.js` 语法错误 = 静默白屏，页面内的 try/catch 救不了自己**（脚本一行都没执行）。
已在 `index.html` 和 `export.py` 模板里各放了一段**独立内联看门狗**：
12 秒后若 `#app` 仍是空的或仍显示载入字样，就区分「脚本没能解析」和「数据没回来」，
显示真实原因与强制刷新指引。三种交付形态都覆盖了。

**3. 存档数据库天天冲突，手工解决极易丢数据。**
工作日构建会把 `cache/announcements.db` 推回仓库，本地跑 `run.py` 也会写它，
每次本地推送都撞二进制冲突（一天里手工救了三次）。
危险在于手工解决时容易顺手「取一边」，而**两边都可能有对方没有的行**——
存档是唯一无法事后重建的东西，取一边就是永久丢数据。
已加 `tools/merge_archive.py` + `.gitattributes` 自动取并集。
⚠️ **git 不信任仓库里的驱动定义，每个新 clone 必须先跑一次**：

    git config merge.archivedb.name "union-merge the announcements archive"
    git config merge.archivedb.driver "python tools/merge_archive.py %O %A %B"

---

## 五、部署现状

| 项 | 值 |
|---|---|
| GitHub | `lokliaoau-bot/asx-radarr`（**公开**仓库） |
| 手机网址 | `https://lokliaoau-bot.github.io/asx-radarr/` |
| 自动更新 | `.github/workflows/daily.yml`：**① 每工作日 UTC 08:30 定时；② 代码一 push 就重新部署；③ 网页手动 Run workflow** |
| 本地 | `python run.py` → 8849，会打印手机可用的局域网地址 |

**用户当前卡在的位置**：代码已推送（commit `40dbf55`），**还差两步、都要在网页上点**：
1. Settings → Pages → Source 选 **GitHub Actions**
2. Actions → 每日更新澳股雷达 → **Run workflow**（首次 3–6 分钟）

首次云端运行没验证过，可能踩坑：Linux 环境差异、依赖版本、Actions 权限。失败要看具体步骤名。

### ⚠️ push 触发有个无限循环陷阱（2026-08-24 加 push 触发时堵的）

这个 workflow 自己会把 `cache/announcements.db` 推回仓库（累积存档，见第四节）。
如果 push 触发不做排除，那次回写会再触发一轮构建、再回写…… **无限循环烧光 Actions 额度**。
两道保险都必须在：
1. `on.push.paths-ignore` 里有 `cache/**`
2. 回写那步的提交信息带跳过标记（`[` + `skip ci` + `]` 那个写法）

**以后改 workflow 千万别把这两条去掉。**

### 💥 加 push 触发时踩的坑（真事，排查了很久）

push 触发加好、代码也推上去了，**Actions 里连一条记录都不生成**。
不是 `paths-ignore` 命中（改动里有 `web/app.js`），不是 Actions 被禁用，
GitHub 的事件 API 里也确实能看到那次 PushEvent。

原因：**GitHub 会扫描本次推送 head commit 的【提交信息全文】**，
一旦出现跳过标记就整个跳过 workflow。而那条提交信息为了「说明」上面这道防循环保险，
**把标记原样写了出来** —— 防循环的保险，把自己防住了。

**规矩：以后在提交信息里提到这个机制，一律用文字描述，不要原样写出那个方括号标记。**
（本文档里也是同样处理，所以上面第2条写得那么别扭。）

### 怎么判断手机上是不是最新版

界面顶部现在有两个时间戳，**含义不同**：
- **生成于 xxx** —— 数据什么时候算的（来自 report.json）
- **界面 xxx** —— 前端代码什么时候构建的（`BUILD_STAMP`，由 `publish.py` / `export.py` 在构建时注入）

两个都要看。以前只有「生成于」，会出现**数据是新的、界面还是上一版**的情况：
装成 App 后 Service Worker 缓存着旧 `app.js`，配上新 `report.json` 照样显示一个很新的「生成于」。
本地跑 `server.py` 时「界面」显示**本地开发版**（源码里是 `@@BUILD@@` 占位符，不替换）。

---

### 三种交付形态（共用一套前端）
- **本地服务器** `python run.py` — 点更新=真的重抓数据+算模型
- **云端 PWA** `python run.py --publish` → `site/` — 手机装主屏幕，点更新=取最新自动结果
- **云盘单文件** `export/ASX市场雷达.html`（约 360KB，全内嵌，双击即开）

`app.js` 先试 `api/report`，失败回落 `report.json`，所以一套前端同时服务本地和静态托管。

---

## 六、代码结构

```
asxquant/
  config.py        153只股票池、11个GICS板块、模型参数
  datafeed.py      Yahoo + ASIC 抓取与缓存
  indicators.py    技术/广度/资金流指标
  features.py      43个市场因子（含空头持仓块、大宗商品块）
  sectors.py       板块合成 + 资金/热度/延展/做空 四个评分
  smartmoney.py    S4吸筹信号 + 顶部资金流入流出面板 + 成本地图组装
                   （indicators.volume_profile 是算法本体）
  announcements.py ASX公告采集 + PDF解析（暂不打分）     ← v2新增
  archive.py       每日快照存档（累积机制核心）          ← v2新增
  picks.py         个股多空评分 + 三只名单
  validate.py      横截面IC / 五分位 / 多空组合验证
  model.py         走向前建模、Platt校准、按技能收缩
  engine.py        编排，产出 JSON
  export.py        单文件HTML导出    publish.py  静态站点生成
  server.py        本地服务（仅标准库，绑 0.0.0.0 供手机访问）
web/               前端 + PWA（manifest / sw.js / 图标）
```

**建模纪律**（全部已实现，别拆）：扩张窗口每21日重训 · 净化禁运 t−h−5 · 三模型等权组合 · 滚动 Platt 校准 · 按实测 AUC 收缩（AUC≤0.5 → λ=0 直接报基准率）。

---

## 七、用户偏好（重要）

1. **不懂金融** — 结论必须大白话，术语用鼠标悬停解释（已实现 `term()` / `annotate()` / `GLOSSARY`）
2. **要诚实** — 没优势就说没优势，绝不制造虚假信心。每个数字旁边附样本外成绩单
3. **一键更新** — 缓存要狠，重复刷新以秒计
4. **跨设备** — 手机 + 云盘都要能看

---

## 八、可以继续做的事

- **等 60 天后**跑公告数据的 F1 领先性检验，通过才纳入评分（这是规格定的判决点）
- 规格里还没做的：F3 板块融资额（配售折价，Appendix 2A/3B）、S6 结构性事件日历
- 规格明确说**不要做**的：ASX 个股期权流（流动性太低）、板块ETF申赎（11个板块只有4个有本土ETF，全球ETF推断澳洲资金流是错的）
- 港股版 `hk-quant` 没有累积机制（南向数据只能回溯64天，同样的问题），可考虑移植 `archive.py`

---

## 九、参考

两份原始规格：`D:\Download\asx-smart-money-detector-spec.md`、`asx-sector-flow-tracker-spec.md`

文献：Welch & Goyal (2008) · Campbell & Thompson (2008) · Rapach, Strauss & Zhou (2010) ·
Gu, Kelly & Xiu (2020) · Asquith, Pathak & Ritter (2005) · Boehmer, Jones & Zhang (2008) ·
Lee & Ready (1991) · Amihud (2002) · Corsi (2009) · López de Prado (2018) · de Kempenaer (RRG)
