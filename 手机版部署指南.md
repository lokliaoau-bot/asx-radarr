# 把澳股雷达装到手机上（云端版）

做完这一份，你在**任何地方、任何手机**打开都能看，图标在主屏幕上，点一下就能取最新数据。
全程免费，一次配置，之后每个交易日自动更新。

---

## 你需要什么

- 一个 GitHub 账号（免费注册：github.com）
- 大约 15 分钟，只做一次

---

> ✅ **第一步已完成**（仓库 `lokliaoau-bot/asx-radarr` 已建好、代码已推送，2026-08-23）。
> 直接从「第二步：打开 GitHub Pages」开始即可。

## 第一步：把代码传到 GitHub（已完成）

1. 打开 https://github.com/new
2. **Repository name** 填 `asx-radarr`
3. 选 **Private**（私有，别人看不到）
   > ⚠️ 注意：私有仓库的 GitHub Pages 需要 Pro 账号。如果你用免费账号，这里要选 **Public**。
   > 选 Public 的话，代码和分析结果是公开的——里面没有你的密码或账户信息，只有公开市场数据，
   > 但**你的选股结论也会公开**。介意的话就用 Pro，或改用"家里WiFi"方案。
4. 点 **Create repository**

然后在电脑上打开命令行，进到项目文件夹执行：

```bash
cd "D:\CLAUDE CODE\asx-quant"
git init
git add .
git commit -m "澳股市场雷达"
git branch -M main
git remote add origin https://github.com/lokliaoau-bot/asx-radarr.git
git push -u origin main
```

（第一次 push 会让你登录，按提示走即可。）

---

## 第二步：打开 GitHub Pages

1. 在仓库页面点 **Settings**（设置）
2. 左边找到 **Pages**
3. **Source** 选 **GitHub Actions**
4. 完成，不用填别的

---

## 第三步：跑第一次

1. 仓库页面点 **Actions**
2. 左边点 **每日更新澳股雷达**
3. 右边点 **Run workflow** → 绿色按钮 **Run workflow**
4. 等 3–6 分钟（第一次要下载 12 年行情 + 5 年空头数据并训练模型）

跑完后，网址是：

```
https://lokliaoau-bot.github.io/asx-radarr/
```

---

## 第四步：装到手机主屏幕

**iPhone（Safari）**
1. Safari 打开上面那个网址
2. 点底部「分享」图标 <kbd>􀈂</kbd>
3. 往下滑，点 **加入主画面 / Add to Home Screen**
4. 点「加入」

**Android（Chrome）**
1. Chrome 打开网址
2. 点右上角三个点
3. 点 **安装应用 / 添加到主屏幕**

装好后主屏幕会出现一个图标，点开就是全屏的 App，看不出是网页。

---

## 平时怎么用

- **点右上角「⟳ 获取最新」** → 立刻取回服务器上最新一次的结果
- 数据**每个交易日澳洲时间傍晚自动更新一次**（UTC 08:30 = 悉尼 18:30/19:30，
  这个时点 ASIC 的空头文件已经发布）
- 想临时手动重算：GitHub 仓库 → Actions → Run workflow（手机浏览器也能点）
- 没网也能打开，显示上次看过的内容

---

## 三种用法的区别

| | 云端手机版 | 家里电脑版 | 云盘单文件 |
|---|---|---|---|
| 在哪都能开 | ✅ | ❌ 要同一WiFi | ✅ |
| 主屏幕图标 | ✅ | ✅ | ❌ |
| 点更新=真的重算 | ❌ 取最新自动结果 | ✅ | ❌ 只读 |
| 自动每日更新 | ✅ | ❌ 手动 | ❌ |
| 需要电脑开着 | ❌ | ✅ | ❌ |

**在家用电脑版**（`python run.py`，手机连同一WiFi也能开，启动时会打印手机用的网址），
**出门用云端版**。两个数据一样，只是更新方式不同。

---

## 常见问题

**Actions 跑失败了？**
点进失败的那次看红色的步骤。最常见是 Yahoo 或 ASIC 临时抽风，隔几分钟重跑一次通常就好。

**手机上打开是白屏？**
等 10 秒——第一次要下载约 300KB 的数据。还是白屏就强制刷新（下拉）。

**能不能不用 GitHub？**
可以用 Cloudflare Pages / Netlify，原理一样：把 `site/` 目录传上去即可。
本地生成命令：`python run.py --publish`，产物在 `site/` 文件夹。

**数据会不会泄露我的信息？**
不会。整个系统只读取公开市场数据（Yahoo 行情、ASIC 公开的空头申报、ASX 公开公告），
不接触任何账户、持仓或密码。
